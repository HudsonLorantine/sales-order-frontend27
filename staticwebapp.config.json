{
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous"],
      "serve": "/api/mockdata.json",
      "statusCode": 200
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*"]
  }
}
